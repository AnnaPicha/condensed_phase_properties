# cp_props.py
def process_data(simulated_data):
    """
    Processes the simulated data for analysis.
    """
    print(f"Processing simulated data: {simulated_data}")
    analyzed_data = simulated_data["result"] * 2
    return analyzed_data
